server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Redirect bare / to the base path
    location = / {
        return 302 $scheme://$http_host/r3sizer/;
    }

    # SPA routing — send all unknown paths to index.html
    location / {
        try_files $uri $uri/ /r3sizer/index.html;
    }

    # Immutable cache for hashed assets (JS, CSS, WASM, fonts)
    location ~* \.(js|css|wasm|woff2?|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
